Todo:

depthmap shader on rendertarget
shadowmap shader with rendertarget as input
discard sun creation from godray shader
improve godray performance
improve water shader
switch from onClientPreRender to onClientHudRender
sun & moon shader (actual is only drawed image temporarely)

known bugs:

cars lost color trough usage of gTexture0
some diffuse light artifacts on some street elements (think its a normal issue)

